# Minimal production dependencies for VisionFlow
# Optimized for smaller Docker images

# Base FastAPI and server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0

# Database
pydantic>=2.5.0
pydantic-settings>=2.1.0
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.7
asyncpg>=0.29.0

# Task queue
redis>=5.0.0
celery>=5.3.0

# Storage
minio>=7.2.0

# Basic image processing (no heavy ML initially)
pillow>=10.1.0
opencv-python-headless>=4.8.0  # headless version is smaller

# Essential ML (CPU-only versions to reduce size)
# Note: Install torch separately with CPU-only flag
numpy>=1.24.0
scipy>=1.11.0

# Monitoring
structlog>=23.2.0
prometheus-client>=0.19.0
prometheus-fastapi-instrumentator>=6.1.0

# Performance
psutil>=5.9.0
gunicorn>=21.2.0
uvloop>=0.19.0

# Text processing
ftfy>=6.1.0

